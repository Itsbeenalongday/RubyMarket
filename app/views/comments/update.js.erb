var html = "<%= j(render(partial: 'comments/update', locals: {comment: @comment, item: @comment.item})) %>"
$('#comment-<%=comment.id%>-body').html(html).append($("#button-group-<%= comment.id %>").html())
$('hr').removeClass('mt-5')
