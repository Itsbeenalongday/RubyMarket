<div class="container my-5">
  <div class="row">
    <div class="card p-5 col-md-6 offset-md-3">
      <h2 class="text-danger">회원정보수정</h2>
      <hr/>
      <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
        <%= render "devise/shared/error_messages", resource: resource, class: "text-danger"  %>

        <div class="form-group">
          <%= f.label :email, class: "text-danger" %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control" %>
        </div>

        <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
          <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
        <% end %>

        <div class="form-group">
          <%= f.label :password, class: "text-danger" %> <i class= "text-danger" >(비밀번호 변경시에만 입력해주세요)</i>
          <%= f.password_field :password, autocomplete: "new-password", class: "form-control" %>
        </div>

        <div class="form-group">
          <%= f.label :password_confirmation, class: "text-danger" %>
          <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "form-control" %>
        </div>

        <div class="form-group">
          <%= f.label :current_password, class: "text-danger" %> <i class= "text-danger" >(정보수정시 입력해주세요)</i>
          <%= f.password_field :current_password, autocomplete: "current-password", class: "form-control" %>
        </div>

        <div class="actions">
          <%= f.submit "정보 수정", class: "btn btn-danger btn-block" %>
        </div>
      <% end %>
      <hr/>
      <h4 class="text-danger">회원 탈퇴</h4>

      <p class="mt-2 text-danger">탈퇴 후엔 모든 정보가 삭제됩니다<%= button_to "회원 탈퇴", registration_path(resource_name), data: { confirm: "정말로 탈퇴하시겠습니까?" }, method: :delete, class: "btn btn-outline-danger btn-block" %></p>
      <hr/>
      <%= link_to "뒤로 가기", :back, class: "btn btn-outline-danger" %>
    </div>
  </div>
</div>